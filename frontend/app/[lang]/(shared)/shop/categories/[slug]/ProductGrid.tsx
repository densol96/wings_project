import { ProductSearchParams } from "@/types/sections/shop";
import React from "react";

const ProductGrid = async ({ page, sortBy }: ProductSearchParams) => {
  return <p>Hello World</p>;
};

export default ProductGrid;
